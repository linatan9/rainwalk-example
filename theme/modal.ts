import type { ComponentTheme, StyledProps } from 'native-base';

const modalContentTheme: ComponentTheme = {
  baseStyle: {
    borderRadius: {
      base: 12,
      md: 16,
    },
  } as StyledProps,
};

export { modalContentTheme };
